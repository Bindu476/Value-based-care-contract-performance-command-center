# Backend + pipelines
fastapi>=0.110
uvicorn>=0.30
pandas>=2.0
polars>=0.20
scikit-learn>=1.4
pyarrow>=15.0
pydantic>=2.0
pyyaml>=6.0
anthropic>=0.20
python-dotenv>=1.0

# Testing
pytest>=8.0
httpx>=0.27

# Notebook + report (optional — only if you want to re-run analysis.ipynb or regenerate figures)
jupyter>=1.0
nbformat>=5.9
matplotlib>=3.8
