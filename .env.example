# Copy this file to .env and fill in real values. .env is gitignored — never commit real secrets.

# Anthropic API key used by backend/app/services/llm_service.py for the Review Brief narrative
# and the AI Insights Q&A feature. Leave unset to run with LLM features disabled (graceful fallback).
ANTHROPIC_API_KEY=
